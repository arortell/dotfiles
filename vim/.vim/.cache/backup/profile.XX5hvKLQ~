# /etc/profile

#Set our umask
umask 022

# Set our default path
PATH="/usr/local/sbin:/usr/local/bin:/usr/bin"
export PATH

# Load profiles from /etc/profile.d
if test -d /etc/profile.d/; then
	for profile in /etc/profile.d/*.sh; do
		test -r "$profile" && . "$profile"
	done
	unset profile
fi

# Source global bash config
if test "$PS1" && test "$BASH" && test -z ${POSIXLY_CORRECT+x} && test -r /etc/bash.bashrc; then
	. /etc/bash.bashrc
fi

# XDG Directories
export XDG_CONFIG_HOME="$HOME/.config"
export XDG_DATA_HOME="$HOME/.local/share"
export XDG_CACHE_HOME="$HOME/.cache"
export XDG_DOWNLOAD_DIR="$HOME/incoming"
export XDG_TRASH_DIR="$HOME/.trash"
export XDG_BIN_DIR="$HOME/bin"
export XDG_TMP_DIR="$HOME/tmp"
export XDG_DESKTOP_DIR="$HOME"
export XDG_TEMPLATES_DIR="$HOME"
export XDG_DOCUMENTS_DIR="/home/Yandex-Disk/docs"
export XDG_PICTURES_DIR="/home/Yandex-Disk/images"

# add some color to grep
export GREP_COLORS='ms=1;37:mc=1;37:sl=:cx=:fn=1;33:ln=1;36:bn=1;35:se=1;30'

# Lemonbar Vars
#PANEL_FIFO="/tmp/panel-fifo"
#PANEL_HEIGHT=22
#PANEL_FONT="Source Code Pro:size=9"
#PANEL_WM_NAME="bspwm_panel"
#export PANEL_FIFO PANEL_HEIGHT PANEL_FONT PANEL_WM_NAME

# BSPWM Vars
export BSPWM_STATE="/tmp/bspwn-state.json"

# SXHKD Shell
export SXHKD_SHELL="/bin/bash"

# Colorize Man Pages
export PAGER="most"

# Set System Wide Editor
export EDITOR="vim"
export VISUAL="vim"
export SUDO_EDITOR="vim"

# Set Browser
export BROWSER="qutebrowser"

# Set Terminal
export TERMINAL="terminology"

# Experimatal
export SUDO_ASKPASS="/usr/bin/daskpass"

# Termcap is outdated, old, and crusty, kill it.
unset TERMCAP

# Man is much better than us at figuring this out
unset MANPATH
